{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
    "build": "vite build",
    "deploy": "yarn build && mkdir dist && copy public\\build\\*.* dist\\",
        "prod": "yarn build && yarn deploy"
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "laravel-vite-plugin": "^1.0",
        "vite": "^5.0"
    }
    
}
